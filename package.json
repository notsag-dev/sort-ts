{
  "name": "sorting-algorithms",
  "version": "1.0.0",
  "description": "Sorting algorithms in typescript",
  "main": "build/app.js",
  "types": "build/app.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json --outDir build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "notsag-dev",
  "license": "GPL-3.0-or-later"
}
